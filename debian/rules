#!/usr/bin/make -f
# -*- makefile -*-
# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

#override_dh_auto_build:
#	dh_auto_build -- CROSS_COMPILE=$(shell dpkg-architecture -qDEB_HOST_MULTIARCH)-

#override_dh_auto_install:
#	dh_auto_install -- CROSS_COMPILE=$(shell dpkg-architecture -qDEB_HOST_MULTIARCH)-

%:
	dh $@
